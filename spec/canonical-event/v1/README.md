# Canonical Event v1

`CanonicalEvent` is the narrow shared event format used for transport, storage, replay, and downstream processing.

The taxonomy intentionally stays compact. Richer semantics belong in derivation layers, not the base event.

