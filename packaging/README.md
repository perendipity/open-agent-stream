# Packaging Templates

- `systemd/oas.service`: Linux service template for `oas daemon run`
- `launchd/dev.open-agent-stream.oas.plist`: macOS launchd template for `oas daemon run`

These templates are starting points. Update the binary path, config path, log
path, and working directory before installing them on a real machine.
