# Portability Goals

The standard should be portable across:

- operating systems
- local directory layouts
- source vendors
- sink vendors
- replay and migration workflows

The repo prioritizes simple on-disk formats, a single-binary collector, and explicit versioned schemas.

