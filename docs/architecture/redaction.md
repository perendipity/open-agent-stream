# Redaction Model

Redaction is policy-driven and per sink.

The initial runtime supports:

- key-based redaction
- regex replacement
- raw payload suppression
- project allow lists
- path deny lists

Redaction is intentionally simple in v0 and should stay replaceable.

