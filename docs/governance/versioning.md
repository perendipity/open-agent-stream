# Versioning Policy

- Spec versions are directory-versioned under `/spec/*/vN`.
- Breaking normative changes require a new versioned directory.
- The Go binary can ship multiple spec versions at once.
- The implementation version must not imply a breaking spec change on its own.

