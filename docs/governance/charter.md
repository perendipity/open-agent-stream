# Project Charter

`open-agent-stream` exists to define and prove an open, replayable, local-first standard for coding-agent session streams.

The project governs:

- normative schemas
- adapter and sink contracts
- conformance expectations
- compatibility rules

The project does not govern downstream product semantics.

